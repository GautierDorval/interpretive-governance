# Deploy on Cloudflare Pages (quick)

1. Cloudflare Dashboard → Workers & Pages → Pages → Create a project
2. Connect to Git → GitHub → select this repository
3. Build settings:
   - Framework preset: None
   - Build command: (empty)
   - Output directory: .
4. Deploy
5. Add custom domain: interpretive-governance.org
